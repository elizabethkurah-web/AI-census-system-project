# Frontend

React-based Progressive Web App (PWA) for data collection.

## Getting Started
1. Install dependencies: `npm install`.
2. Start development server: `npm start`.
3. Build for production: `npm run build`.

> This is a placeholder; update after scaffolding actual frontend code.